\documentclass[conference]{IEEEtran}

\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{url}

\graphicspath{{figures/}}

\title{GNN--BERT for Understanding Context from Music}

\author{\IEEEauthorblockN{Moin Mostakim}
\IEEEauthorblockA{Department of Computer Science and Engineering\\
Course: Neural Networks (CSE425 / EEE474 / CSE715)\\
Email: \textit{[add institutional email]}}}

\begin{document}
\maketitle

\begin{abstract}
Music context is distributed across acoustic structure and language. This
paper presents a three-stage study of text-only, audio-graph, and graph--text
models for music understanding. Task 1 fine-tunes DistilBERT for a
MusicCaps caption-to-tag proxy task. Task 2 represents FMA-small tracks as
graphs of five-second audio segments and compares a GraphSAGE classifier with
a mel-spectrogram CNN. Task 3 predicts a fixed multi-label genre/tag
vocabulary from FMA metadata text and audio graphs, comparing BERT-only,
GNN-only, early-concatenation, and cross-attention fusion. On the reported
test runs, the Task 2 CNN obtains macro-F1 $0.4544$ versus $0.4323$ for
GraphSAGE. In Task 3, GNN-only obtains the best macro-F1 ($0.0427$) and
mean PR-AUC ($0.1340$), while cross-attention obtains the best micro-F1
($0.3546$). The results show that graph structure supplies useful signal for
the selected FMA labels, but also expose substantial generalization
difficulties for the multi-label fusion setting.
\end{abstract}

\begin{IEEEkeywords}
music understanding, GraphSAGE, BERT, multimodal learning, audio graphs,
multi-label classification
\end{IEEEkeywords}

\section{Introduction}
Music context includes timbre, rhythm, harmony, genre, mood, and natural
language descriptions. Audio-only sequence models capture local patterns but
do not explicitly represent relations between repeated or similar segments.
Conversely, a language encoder captures contextual semantics but cannot
directly observe acoustic structure. We study whether a graph representation
of audio segments and a BERT representation of metadata text can provide
complementary information.

The project has three progressively more structured tasks. First, a
DistilBERT baseline measures text-only performance on a caption-to-tag proxy
task. Second, a GraphSAGE model performs single-label genre classification
from audio segment graphs and is compared with a mel-spectrogram CNN.
Third, a graph--text model performs multi-label genre/tag prediction and is
evaluated through a four-condition ablation. The report follows the IEEE
conference format and reports only the real-run artifacts in the repository;
synthetic smoke-test metrics are excluded.

\section{Related Work}
The language branch is based on the contextual representation strategy of
BERT \cite{devlin2019bert}; the implementation uses the smaller
DistilBERT configuration for the text-only baseline
\cite{sanh2019distilbert}. GraphSAGE learns node representations by sampling
and aggregating neighborhood information \cite{hamilton2017inductive}.
The audio features use standard short-time spectral analysis and
mel-spectrogram conventions \cite{mcfee2015librosa}. FMA provides the
audio and metadata basis for Tasks 2--3 \cite{defferrard2017fma}, while
MusicCaps supplies natural-language descriptions for the Task 1 proxy
experiment \cite{agostinelli2023musiclm}.

\section{Project Scope and Data}
\subsection{Datasets and targets}
Task 1 uses real MusicCaps captions and a proxy tag target generated by the
documented MusicCaps workflow. This target is not a human-annotated
MusicCaps tagging benchmark, so its score is reported separately from the
FMA experiments. Tasks 2 and 3 use FMA-small. Task 2 is single-label
classification over the eight top-level genres used by the implementation:
Electronic, Experimental, Folk, Hip-Hop, Instrumental, International, Pop,
and Rock.

Task 3 uses the complete FMA metadata source and constructs a fixed
multi-label vocabulary. Normalized genres are included first and frequent
normalized tags fill the remaining positions up to the configured maximum
of 100 labels. The target is multi-hot: a track is positive for its normalized
top genre and any selected normalized tags. The exact vocabulary is persisted
by the preparation pipeline and reused across all four ablation conditions.

\subsection{Splits and leakage control}
The Task 3 metadata decisions require an artist-disjoint partition: all tracks
from one artist belong to exactly one of train, validation, or test. The
fusion manifest is an inner join on \texttt{track\_id} between the graph
manifest and prepared text metadata, so failed graph construction and rows
without usable text are excluded consistently. Feature scaling is fitted on
training segments and shared across splits. These choices prevent direct
artist leakage and prevent test statistics from influencing graph features.

\section{Method}
\subsection{Audio segmentation and graph construction}
Audio is loaded as mono at 22{,}050 Hz and partitioned into five-second
segments. Each segment node contains MFCC means and standard deviations,
first- and second-order MFCC deltas, chroma statistics, spectral contrast,
tonnetz, spectral centroid, bandwidth, rolloff, zero-crossing rate, RMS
energy, and spectral-flatness statistics. The resulting feature dimension is
182. Temporal-adjacency edges connect consecutive segments. Additional
undirected similarity edges are added when the cosine similarity of the
complete audio feature vectors is at least $0.75$.

The graph encoder first projects node features to a 256-dimensional hidden
space, applies three residual GraphSAGE layers with normalization and
dropout, and concatenates mean, maximum, and standard-deviation global
readouts. A linear head maps the graph representation to the target labels.
The Task 2 baseline instead applies a compact CNN to a 128-bin log-mel
spectrogram.

\subsection{Text and fusion models}
Task 1 uses a DistilBERT encoder with a linear multi-label head. Task 3
passes only non-target FMA metadata text to BERT: album information, album
title/type, artist biography/location/members, composer, track information,
and track title. Genre/tag fields, identifiers, dates, URLs, and split
metadata are excluded from the text input.

Let $g$ denote the graph readout and $H$ the BERT token sequence. The
cross-attention condition uses a graph-derived query:
\begin{equation}
 A = \operatorname{softmax}\left(\frac{(gW_Q)(HW_K)^\top}{\sqrt{d}}\right),
 \qquad
 z = [g;\, AHW_V].
\end{equation}
The early-concatenation condition replaces the attended text vector with the
BERT pooled output. The BERT-only and GNN-only conditions remove the other
branch. All Task 3 conditions use binary cross-entropy with logits, and
predictions are thresholded at $0.5$ for F1.

\subsection{Evaluation}
We report macro-F1, micro-F1, and mean average precision (reported by the
code as mean PR-AUC) for multi-label tasks. Task 2 additionally reports
accuracy and test loss. Checkpoints are selected using the validation
procedure in the training scripts; the tables below contain held-out test
metrics from the exported JSON artifacts.

\section{Experimental Evolution and Reproducibility}
\subsection{From smoke tests to real experiments}
The project was developed in stages rather than by training a single final
model. The initial scaffold included a synthetic graph generator and a
smoke-test workflow. Synthetic samples used six controlled labels, small
random node features, deterministic splits, and serialized graph manifests.
This stage verified tensor shapes, checkpointing, evaluation, and
case-study export. Synthetic outputs were deliberately excluded from the
scientific results reported here.

The next stage established the real text baseline. A MusicCaps download and
preparation path was added, followed by stable hashing into 80/10/10
train/validation/test partitions and DistilBERT fine-tuning. The project
requirements then motivated the dataset decision: FMA-small was selected for
the audio graph and fusion tasks, while MusicCaps was retained for the
text-only proxy task. The repository was reorganized so raw data, processed
features, split files, source modules, and task-specific results were
separate and reproducible.

Task 2 progressed through several engineering iterations. The pipeline first
prepared FMA metadata and segment graphs, then separated mel-spectrogram
caching from training, added batched CNN segment processing, expanded node
features beyond MFCCs, corrected feature normalization before similarity
construction, added per-file failure reports, and upgraded the GraphSAGE
encoder and readout. Learning curves, confusion matrices, predictions, and
an explicit CNN/GraphSAGE comparison were added after the held-out
evaluation path was established.

Task 3 was then isolated from the Task 2 trainer. Its development introduced
a scan--select--prepare label workflow, explicit parsing of array-valued FMA
tags, deterministic vocabulary selection, labeled text construction, an
inner graph/text manifest join, leakage checks, positive class weighting,
progress reporting, and separate training commands for all ablation
conditions. The final analytics command reads completed runs without
retraining and produces rankings, metric deltas, plots, predictions, and
case studies.

\begin{table*}[t]
\caption{Development milestones and preserved evidence.}
\label{tab:milestones}
\centering
\begin{tabular}{p{0.17\textwidth}p{0.27\textwidth}p{0.46\textwidth}}
\toprule
Stage & Research question & Preserved evidence\\
\midrule
Synthetic smoke test & Can graph/text training and evaluation run end to end? &
\texttt{src/make\_synthetic\_data.py}, synthetic manifest, checkpoint and case-study workflow\\
Task 1 baseline & Can contextual text encode the documented proxy labels? &
MusicCaps preparation, DistilBERT checkpoint, metric history, predictions, F1 curve\\
Dataset/layout decisions & Which datasets and directory contracts are valid? &
Requirements PDF, README, Task 2/3 decision records, task-specific processed/results paths\\
Task 2 preprocessing & Can real audio become leakage-controlled graphs and cached mels? &
FMA metadata, audio features, scaling, graph manifest, split files, failure report, mel cache\\
Task 2 modeling & Does graph structure beat a standard spectrogram baseline? &
GraphSAGE/CNN checkpoints, learning curves, confusion matrices, predictions, comparison JSON\\
Task 3 preparation & Can labels and text be built without target leakage? &
Scan/select/prepare commands, fixed vocabulary, prepared text, inner-join manifest, validators\\
Task 3 ablation & Which modality and fusion mechanism contributes? &
Four checkpoints, histories, predictions, PR curves, t-SNE plots, case studies, comparison analysis\\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Configuration and reproducibility}
Unless overridden by a task command, experiments use seed 42, batch size 8,
learning rate $3\times10^{-4}$, weight decay $10^{-4}$, dropout 0.2, three
GraphSAGE layers, 256-dimensional graph and text projections, gradient
clipping at 1.0, a factor-0.5 learning-rate scheduler, and early stopping
with patience 12. Task 1 uses DistilBERT, maximum sequence length 128,
batch size 8, learning rate $2\times10^{-5}$, and the documented real run.
Task 2 and Task 3 use the same seed/configuration contract; Task 3 runs are
capped at 20 epochs and select the best validation Macro-F1 checkpoint.
The primary Task 3 fusion run uses positive weighting and every ablation
uses the same fixed label order.

\begin{table}[t]
\caption{Core experimental hyperparameters.}
\label{tab:hyperparameters}
\centering
\begin{tabular}{ll}
\toprule
Parameter & Value\\
\midrule
Seed & 42\\
Audio sample rate / segment & 22,050 Hz / 5 s\\
Mel bins / similarity threshold & 128 / 0.75\\
Graph layers / hidden size & 3 / 256\\
Text model / max length & DistilBERT / 128\\
Dropout / batch size & 0.2 / 8\\
Learning rate / weight decay & $3\times10^{-4}$ / $10^{-4}$\\
Scheduler / early stopping & factor 0.5 / patience 12\\
Gradient clipping & 1.0\\
Task 3 loss & BCEWithLogitsLoss\\
\bottomrule
\end{tabular}
\end{table}

\section{Experiments and Results}
\subsection{Task 1: DistilBERT proxy baseline}
The exported Task 1 test artifact reports macro-F1 $0.9976$, micro-F1
$0.9991$, and PR-AUC $1.0000$. Because the experiment uses caption-derived
proxy labels, these values should be interpreted as a verification of the
text pipeline and not as evidence of general MusicCaps tag understanding.

\begin{table}[t]
\caption{Task 1 test performance on the MusicCaps proxy task.}
\label{tab:task1}
\centering
\begin{tabular}{lccc}
\toprule
Model & Macro-F1 & Micro-F1 & PR-AUC\\
\midrule
DistilBERT & 0.9976 & 0.9991 & 1.0000\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Task 2: Audio graph classification}
Table~\ref{tab:task2} compares the audio graph model with the mel-CNN.
The CNN leads GraphSAGE by 0.0222 macro-F1 and 0.0350 PR-AUC, while both
models achieve their best per-class F1 on Hip-Hop. GraphSAGE is stronger on
International, whereas the CNN is stronger on Experimental, Folk, Hip-Hop,
Instrumental, Pop, and Rock.

\begin{table}[t]
\caption{Task 2 FMA-small genre classification test results.}
\label{tab:task2}
\centering
\begin{tabular}{lccccc}
\toprule
Model & Acc. & Macro-F1 & Micro-F1 & PR-AUC & Loss\\
\midrule
Mel-CNN & 0.4650 & 0.4544 & 0.4650 & 0.4939 & 1.4877\\
GraphSAGE & 0.4300 & 0.4323 & 0.4300 & 0.4534 & 1.7490\\
\bottomrule
\end{tabular}
\end{table}

\begin{table*}[t]
\caption{Task 2 per-genre test F1. Bold values indicate the stronger model
for each genre; the final column is CNN minus GraphSAGE.}
\label{tab:task2-perclass}
\centering
\begin{tabular}{lccc}
\toprule
Genre & Mel-CNN & GraphSAGE & $\Delta$\\
\midrule
Electronic & 0.5510 & \textbf{0.5771} & -0.0261\\
Experimental & \textbf{0.3814} & 0.2880 & +0.0934\\
Folk & \textbf{0.2647} & 0.2513 & +0.0134\\
Hip-Hop & \textbf{0.7444} & 0.6900 & +0.0544\\
Instrumental & \textbf{0.4330} & 0.3209 & +0.1121\\
International & 0.3467 & \textbf{0.4852} & -0.1385\\
Pop & 0.3333 & \textbf{0.3491} & -0.0157\\
Rock & \textbf{0.5809} & 0.4967 & +0.0842\\
\midrule
Macro-F1 & \textbf{0.4544} & 0.4323 & +0.0222\\
\bottomrule
\end{tabular}
\end{table*}

\noindent
The per-class table is more informative than accuracy alone: GraphSAGE
improves Electronic, International, and Pop, while the CNN has larger gains
on Experimental, Instrumental, and Rock. Both models are strongest on
Hip-Hop, suggesting that class separability and class frequency are not
uniform across the eight genres.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{task2_cnn_learning_curves.png}
\caption{Task 2 CNN learning curves; the companion GraphSAGE curves and
confusion matrices are included in the uploaded figure assets.}
\label{fig:task2}
\end{figure}

\subsection{Task 3: Fusion ablation}
The four-condition results are shown in Table~\ref{tab:task3}. GNN-only
achieves the highest macro-F1 and PR-AUC. Cross-attention achieves the
highest micro-F1, narrowly exceeding early concatenation. Both fusion
conditions substantially improve on BERT-only micro-F1 and PR-AUC, but their
macro-F1 remains low, indicating that performance is concentrated in a
subset of labels. The gap between training and test metrics in the exported
histories also indicates overfitting.

\begin{table}[t]
\caption{Task 3 FMA multi-label ablation test results.}
\label{tab:task3}
\centering
\begin{tabular}{lccc}
\toprule
Condition & Macro-F1 & Micro-F1 & PR-AUC\\
\midrule
BERT-only & 0.0111 & 0.2030 & 0.0366\\
GNN-only & \textbf{0.0427} & 0.3353 & \textbf{0.1340}\\
Early concat. & 0.0403 & 0.3542 & 0.1255\\
Cross-attention & 0.0393 & \textbf{0.3546} & 0.1308\\
\bottomrule
\end{tabular}
\end{table}

\begin{table*}[t]
\caption{Task 3 improvement over the BERT-only test baseline. Positive
values are improvements for F1 and PR-AUC; for loss, negative values would
be improvements.}
\label{tab:task3-deltas}
\centering
\begin{tabular}{lrrrr}
\toprule
Condition & $\Delta$ Macro-F1 & $\Delta$ Micro-F1 & $\Delta$ PR-AUC & $\Delta$ Loss\\
\midrule
BERT-only & 0.0000 & 0.0000 & 0.0000 & 0.0000\\
GNN-only & +0.0315 & +0.1323 & +0.0974 & +1.3756\\
Early concat. & +0.0292 & +0.1512 & +0.0889 & +0.4578\\
Cross-attention & +0.0281 & \textbf{+0.1516} & +0.0942 & +0.6663\\
\bottomrule
\end{tabular}
\end{table*}

\noindent
This delta table is included because the absolute Task 3 scores alone hide
the practical effect of adding audio. Cross-attention gives the largest
micro-F1 improvement, while GNN-only gives the largest macro-F1 and PR-AUC
improvements. None of the audio-containing conditions minimizes BCE test
loss, so the result is not a uniformly dominant fusion model.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{task3_ablation_comparison.png}
\caption{Exported comparison of the four Task 3 conditions.}
\label{fig:ablation}
\end{figure}

\subsection{Training histories and post-ablation comparison}
The exported histories provide evidence beyond the final test row. Task 1
reaches its best validation Macro-F1 of 0.0438 at epoch 4 in the stored
history. In Task 3, all three non-BERT conditions reach their best stored
validation Macro-F1 at epoch 5, while BERT-only reaches its best at epoch 2.
The large train/test gaps, especially for GNN-only and fusion, indicate
overfitting and label imbalance rather than a lack of optimization alone.

Relative to BERT-only, the post-ablation analysis reports the following
cross-attention changes: Macro-F1 +0.0281, micro-F1 +0.1516, PR-AUC
+0.0942, and test loss +0.6663. Thus adding audio improves micro-level
decisions and ranking quality, but the improvement is not uniform over
labels. The ranking is metric-dependent: GNN-only is best for Macro-F1 and
PR-AUC, cross-attention is best for micro-F1, and BERT-only has the lowest
test loss.

\begin{table}[t]
\caption{Best validation epochs recorded in exported histories.}
\label{tab:epochs}
\centering
\begin{tabular}{lcc}
\toprule
Run & Best epoch & Test Macro-F1\\
\midrule
Task 1 DistilBERT & 4 & 0.0409\\
Task 3 BERT-only & 2 & 0.0111\\
Task 3 GNN-only & 5 & 0.0427\\
Task 3 early concat. & 5 & 0.0403\\
Task 3 cross-attention & 5 & 0.0393\\
\bottomrule
\end{tabular}
\end{table}

\section{Qualitative Analysis}
The repository exports t-SNE projections of fused embeddings, learning
curves, and precision--recall curves for each ablation. These plots are
included as supplementary figures in the Overleaf package. The t-SNE views
should be read descriptively rather than as a quantitative metric: they
visualize whether embedding neighborhoods appear label-structured, but do
not replace held-out evaluation.

Three case studies are exported for the full cross-attention run. Each
case study contains the track identifier, text input, target labels,
predicted labels, and prediction scores. These examples are intended to
support inspection of graph/text agreement and failure modes. In particular,
the low macro-F1 of all Task 3 conditions motivates checking rare-label
misses rather than presenting only successful examples.

\section{Discussion}
The results provide three practical conclusions. First, the text pipeline is
functional, but the Task 1 proxy construction makes its near-perfect score
an upper-bound-like diagnostic rather than a comparable benchmark. Second,
the rich segment graph is competitive with a simple audio CNN but does not
outperform it on the current FMA-small run; graph topology or the similarity
threshold may require further tuning. Third, the Task 3 ablation confirms
that audio structure is the dominant signal for this label setup. Text
fusion improves micro-level decisions over BERT-only, yet does not improve
macro-F1 over GNN-only, suggesting label imbalance, limited usable text, or
optimization difficulty.

\section{Limitations and Future Work}
This study does not report the optional emotion head or the advanced
MusicCaps contrastive retrieval task. The original requirements also describe
DEAM valence/arousal regression and cross-modal retrieval; these remain
planned extensions rather than completed experiments. Task 1 and Tasks 2--3
use different supervision and should not be compared directly. The repository
also does not commit a full-data inventory table for exact track counts, so
those counts should be inserted from the local preparation logs before final
submission.

The project has additional practical limitations. The Task 1 target is a
caption-derived proxy, FMA metadata text is sparse and heterogeneous, and
the selected Task 3 labels are imbalanced. The 0.5 threshold is global rather
than label-calibrated. The graph contains rich handcrafted statistics, but
the current study does not isolate each feature family or compare multiple
similarity thresholds. Finally, the reported runs use one seed; repeated-seed
confidence intervals are needed before claiming robust ranking differences.

\subsection{Future-work roadmap}
The next experiments follow directly from the preserved findings:
\begin{enumerate}
\item add per-label validation threshold tuning, calibrated probabilities,
and repeated seeds with confidence intervals;
\item compare class-balanced, focal, and asymmetric multi-label losses and
report rare-label versus frequent-label performance;
\item sweep graph similarity thresholds, adjacency variants, pooling
strategies, and feature ablations;
\item improve text coverage while maintaining strict exclusion of genre/tag
targets and artist identity shortcuts;
\item add the optional DEAM emotion head as a genuinely multi-task objective;
\item implement paired graph--caption contrastive learning and report
Caption-to-Audio and Audio-to-Caption Recall@1/5/10;
\item expand qualitative analysis with graph paths, attention maps, and
failure cases from all four ablations; and
\item package exact dataset counts, vocabulary frequencies, split summaries,
and preprocessing logs as supplementary reproducibility tables.
\end{enumerate}

\section{Artifact Map for Reproduction}
The report is backed by task-specific artifacts rather than screenshots alone.
Task 1 uses the metrics, test-metrics, predictions, and F1-curve files under
\texttt{results/task1}. Task 2 uses both model metric files, predictions,
learning curves, confusion matrices, and
\texttt{task2\_model\_comparison.json}. Task 3 uses one metrics, prediction,
case-study, learning-curve, PR-curve, and t-SNE bundle per condition, plus
\texttt{task3\_ablation\_comparison.json} and
\texttt{task3\_ablation\_analysis.txt}. Source modules under \texttt{src/}
define the preprocessing and training contracts, while \texttt{README.md}
contains the sequential commands needed to regenerate the pipeline.

\section{Conclusion}
We implemented a reproducible BERT, GraphSAGE, and graph--text fusion
pipeline for music context understanding. On the current real-run outputs,
the mel-CNN is the strongest Task 2 classifier, while graph representations
are the strongest Task 3 source of macro-level multi-label signal. The
ablation results support multimodal fusion as a useful direction, but also
make clear that future gains require stronger supervision and improved
generalization to rare labels.

\appendices
\section{Complete Plot Archive}
The following figures are included so that the experimental work is retained
in the Overleaf project rather than reduced to the summary tables. The
learning curves show optimization histories, the precision--recall figures
show per-label ranking behavior, the t-SNE figures show test embedding
geometry, and the confusion matrices show class-specific Task 2 errors.

\begin{figure*}[t]
\centering
\includegraphics[width=.48\textwidth]{task1_distilbert_f1_curve.png}
\includegraphics[width=.48\textwidth]{task2_cnn_confusion_matrix.png}
\includegraphics[width=.48\textwidth]{task2_graphsage_confusion_matrix.png}
\includegraphics[width=.48\textwidth]{task2_cnn_learning_curves.png}
\includegraphics[width=.48\textwidth]{task2_graphsage_learning_curves.png}
\includegraphics[width=.48\textwidth]{task3_ablation_comparison.png}
\caption{Task 1, Task 2, and post-ablation comparison plots.}
\label{fig:archive-overview}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=.48\textwidth]{task3_bert_only_learning_curves.png}
\includegraphics[width=.48\textwidth]{task3_bert_only_pr_curve.png}
\includegraphics[width=.48\textwidth]{task3_bert_only_fused_tsne.png}
\includegraphics[width=.48\textwidth]{task3_gnn_only_learning_curves.png}
\includegraphics[width=.48\textwidth]{task3_gnn_only_pr_curve.png}
\includegraphics[width=.48\textwidth]{task3_gnn_only_fused_tsne.png}
\caption{BERT-only and GNN-only ablation diagnostics.}
\label{fig:archive-single}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=.48\textwidth]{task3_early_concat_learning_curves.png}
\includegraphics[width=.48\textwidth]{task3_early_concat_pr_curve.png}
\includegraphics[width=.48\textwidth]{task3_early_concat_fused_tsne.png}
\includegraphics[width=.48\textwidth]{task3_cross_attention_learning_curves.png}
\includegraphics[width=.48\textwidth]{task3_cross_attention_pr_curve.png}
\includegraphics[width=.48\textwidth]{task3_cross_attention_fused_tsne.png}
\caption{Early-concatenation and cross-attention diagnostics.}
\label{fig:archive-fusion}
\end{figure*}

\bibliographystyle{IEEEtran}
\bibliography{references}
\end{document}
